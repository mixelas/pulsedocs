version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: pulsedocs:latest
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
    ports:
      - "3000:3000"
    restart: unless-stopped
